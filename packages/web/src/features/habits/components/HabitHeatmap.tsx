import React from 'react';

// TODO: Implement 7x53 heatmap grid with 4-level colour scale
// Must render exactly 365 cells (one per calendar day in [today-364, today])
export function HabitHeatmap(): React.JSX.Element {
  return <div>HabitHeatmap — not yet implemented</div>;
}
